[pytest]
# The venv and the model's build products hold thousands of files pytest would
# otherwise walk, including other projects' tests.
norecursedirs = .git .venv model/.venv model/dataset/images model/training/v1 build install log *.egg-info
testpaths = src model/capture analysis
