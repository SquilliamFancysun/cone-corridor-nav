# Written by roboflow_export.py — the committed record of the
# dataset this model was trained on. Images are gitignored, so the
# paths point at a local export; re-download it with
#   python roboflow_export.py --version N
path: ../export/cone-detector-nfjog-v3
train: train/images
val: valid/images
test: test/images
nc: 5
names:
  0: blue
  1: magenta
  2: orange
  3: red
  4: yellow
